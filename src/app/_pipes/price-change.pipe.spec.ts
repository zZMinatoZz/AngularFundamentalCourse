import { PriceChangePipe } from './price-change.pipe';

describe('PriceChangePipe', () => {
  it('create an instance', () => {
    const pipe = new PriceChangePipe();
    expect(pipe).toBeTruthy();
  });
});
